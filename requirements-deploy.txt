# Lean runtime deps for the API/bot deploy (NOT the heavy training/ingest stack).
# LightGBM/SHAP/Telethon/PyMuPDF are intentionally excluded — the served model is the
# scikit-learn HistGradientBoosting artifact, and scoring needs none of the rest.
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
duckdb>=1.0
pandas>=2.2
scikit-learn>=1.5
joblib>=1.3
rapidfuzz>=3.9
pyyaml>=6.0
typer>=0.12
